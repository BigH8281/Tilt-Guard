# Set this when the frontend talks to an API on a different origin or path.
# Examples: https://api.example.com or /api
# If the frontend is served behind the same origin as the API root, it can be omitted.
VITE_API_BASE_URL=https://api.example.com
